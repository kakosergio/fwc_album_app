import 'package:flutter/material.dart';

class StickerPercent extends StatelessWidget {
  const StickerPercent({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack();
  }
}
